name: Reveal secret
on: workflow_dispatch

jobs:
  reveal:
    runs-on: ubuntu-latest
    steps:
      - name: Encode secret
        env:
          MY_SECRET: ${{ secrets.MY_SECRET }}
        run: echo "$MY_SECRET" | base64